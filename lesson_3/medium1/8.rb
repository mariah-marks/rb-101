# The method invocation `rps("rock", "paper")` returns "paper" and the method
# invocation `rps("rock", "scissors")` returns "rock".

# The method call `rps("paper", "rock")` returns "paper"

# The method call `rps("paper", "rock")` returns "paper", which is passed to the
# `puts` method.

# paper
# => nil
