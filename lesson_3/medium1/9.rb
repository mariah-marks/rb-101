# The return value of `foo` a string object with the value "yes". Passed as an
# argument to the `bar` method, the default value of method local variable
# `param` is overridden, and the ternary statement to returns the string "no".
# The `puts` method is invoked and passed "no" as an argument.

# no
# => nil
