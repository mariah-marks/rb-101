# To add and array element to the array referenced by `my_array`, we need to
# replace the Array#<< method with a method that returns a new array object,
# like Array#+.

# an_array_param += ["rutabaga"]
